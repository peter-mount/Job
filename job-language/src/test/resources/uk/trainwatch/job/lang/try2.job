// test that try actually captures an exception
// this also tests throw
{
    try {
        log "body";
        throw new Exception();
        log "throw failed";
    } catch( Exception ex ) {
        log "Exception caught";
    }
}
